    {% extends "base.html.twig" %}
    {% block megatitle %} Articles {% endblock megatitle %}




    {% block main %}

    {% for article in articles %}
        <article class="article-container" >
            <h2>{{ article.title }}</h2>
            <a href="{{ path("article_show", {id: article.id}) }}">Voir le produit</a>
        </article>
    {% endfor %}

    {% endblock main %}

